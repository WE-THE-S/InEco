#include <Arduino.h>
#include "./config.hpp"
#include "./struct/packet.hpp"

void setup() {
  #if defined(LED_BOARD)
    
  #elif defined(CONTROL_BOARD)
    
  #endif
}

void loop() {
  #if defined(LED_BOARD)
  #elif defined(CONTROL_BOARD)
    
  #endif
}